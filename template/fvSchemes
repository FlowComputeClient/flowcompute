/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  %1|
|   \\  /    A nd           | Website:  %2|
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      fvSchemes;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

// Time-derivative schemes
ddtSchemes
{
    default          %3;
}

// Configures how cell-centered gradients are calculated
gradSchemes
{
    default          Gauss linear;
}

// Configures how convective terms are discretized
divSchemes
{
    default          none;
    div(phi,U)       %4;
    div(div(phi,U))  Gauss linear;
    div((nuEff*dev2(T(grad(U))))) Gauss linear;
    div(phi,k)       bounded Gauss upwind;
    div(phi,epsilon) bounded Gauss upwind;
    div(phi,omega)   bounded Gauss upwind;
    div(phi,h)       bounded Gauss upwind;
    div(phi,e)       bounded Gauss upwind;
    %5
}

// Configures how diffusion terms are discretized
laplacianSchemes
{
    default          Gauss linear %6;
}

// Specifies how cell-centered values are interpolated
interpolationSchemes
{
    default          linear;
}

// Configures how surface normal gradients are evaluated
snGradSchemes
{
    default          %6;
}

// Computes distance to the nearest wall
wallDist
{
    method           meshWave;
}

// ************************************************************************* //